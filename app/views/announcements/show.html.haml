%h3
  = announcement.title
  =link_to "Edit", edit_announcement_path(announcement), class: "btn btn-warning"
%br
%h4= announcement.created_at
%p= announcement.description
=link_to "Back", announcements_path, class: "btn btn-primary"
